<div>
    <header class="pta-id-card pta-actor-sheet-header pta-sheet-header" data-type-main="{{system.types.primary}}" data-type-extra="{{system.types.secondary}}">
        <div class="flexrow flex-gap-m">
            <img class="flex0" data-action="editImage" width="100" height="100" src="{{document.img}}">
            <div class="flexcol flex-gap-m">
                <input class="pta-actor-name" type="text" value="{{document.name}}" name="name">
                <div class="flexrow">
                    <div class="flexrow flex-gap-s">

                        <div class="pta-type-tag pta-type-{{system.types.primary}} pta-s">
                            <div class="wrapper">
                                {{systemFieldInput system 'types.primary'}}
                            </div>
                        </div>

                        <div class="pta-type-tag pta-type-{{system.types.secondary}} pta-s">
                            <div class="wrapper">
                                {{systemFieldInput system 'types.secondary'}}
                            </div>
                        </div>

                    </div>
                </div>
                <div class="progress-bar">
                    <label>Health</label>
                    <div class="wrapper">
                        <div class="bar {{#if (lt (divide system.hp.value system.hp.max) 0.33)}}red{{else}}green{{/if}}" style="width: {{multiply 100 (divide system.hp.value system.hp.max)}}%;">
                        </div>
                        <div class="flexrow pta-s">
                            {{systemFieldInput system 'hp.value'}}/
                            {{#if isEditMode}}
                                {{systemFieldInput system 'hp.base'}}
                            {{else}}
                                <div class="text-center">{{system.hp.max}}</div>
                            {{/if}}
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </header>

    {{> "pta.tabs-content"}}
</div>